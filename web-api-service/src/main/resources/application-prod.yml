spring:
  config:
    activate:
      on-profile: prod
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 2000
            readTimeout: 3000
            loggerLevel: BASIC

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false